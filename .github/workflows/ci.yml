name: ci
on:
  push:
  pull_request:

jobs:
  build:
    name: Build + gate
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Pin Rust 1.98.0
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.98.0"
          components: clippy, rustfmt
      - name: Verify lockfile is up-to-date
        run: >
          cargo metadata --locked >/dev/null ||
          { echo 'Lockfile out of date: run cargo update --workspace and commit Cargo.lock' >&2; exit 1; }
      - name: Enforce dependency edge ownership
        run: cargo run --locked -p lgwks_deps --bin lgwks-deps -- check .
      - name: Forbid unwrap outside tests
        # Safety bar: library code fails loud with typed errors or named
        # expects, never bare unwrap. Doc examples count as shipped surface.
        run: |
          set -eu
          tmp="$(mktemp)"
          trap 'rm -f "$tmp"' EXIT
          grep -rn '\.unwrap()' crates/*/src --include='*.rs' > "$tmp" || true
          fail=0
          while IFS= read -r rec; do
            loc="${rec%%:*}"; rest="${rec#*:}"; line="${rest%%:*}"
            top="$(grep -n 'mod tests' "$loc" | head -1 | cut -d: -f1)"
            if [ -z "$top" ] || [ "$line" -lt "$top" ]; then
              echo "unwrap outside tests: $loc:$line" >&2
              fail=1
            fi
          done < "$tmp"
          [ "$fail" -eq 0 ] && echo "no unwrap outside tests"
          exit "$fail"
      - name: Build all workspace targets
        run: cargo test --workspace --all-targets --locked --no-run

  fmt:
    name: Format
    needs: [build]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Pin Rust 1.98.0
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.98.0"
          components: rustfmt
      - name: Check formatting
        run: cargo fmt --all -- --check

  tests:
    name: Tests
    needs: [build]
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Pin Rust 1.98.0
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.98.0"
          components: clippy
      - name: Run all workspace tests
        run: cargo test --workspace --all-targets --locked
      - name: Run lgwks-bot no-default-feature tests
        run: cargo test -p lgwks_bot --all-targets --locked --no-default-features
      - name: Run lgwks-std full-feature tests
        run: cargo test -p lgwks_std --all-targets --locked --features full
      - name: Run lgwks-bot async runner tests
        run: cargo test -p lgwks_bot --all-targets --locked --features full
      - name: Doc tests
        run: cargo test --workspace --doc --locked

  clippy:
    name: Clippy
    needs: [build]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Pin Rust 1.98.0
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.98.0"
          components: clippy
      - name: Clippy all workspace targets
        run: cargo clippy --workspace --all-targets --locked -- -D warnings
      - name: Clippy feature matrix
        # Per-crate all-features (gpui excluded: it needs the Xcode Metal
        # toolchain and is covered by the gpui-macos job, not ubuntu lanes).
        run: >
          cargo clippy --locked --manifest-path crates/lgwks-std/Cargo.toml
          --all-targets --all-features -- -D warnings &&
          cargo clippy --locked --manifest-path crates/lgwks-bot/Cargo.toml
          --all-targets --all-features -- -D warnings &&
          cargo clippy --locked --manifest-path crates/lgwks-ast/Cargo.toml
          --all-targets --all-features -- -D warnings &&
          cargo clippy --locked --manifest-path crates/lgwks-deps/Cargo.toml
          --all-targets --features tokio-full -- -D warnings
      - name: Clippy no default features
        run: >
          cargo clippy --locked --manifest-path crates/lgwks-std/Cargo.toml
          --all-targets --no-default-features -- -D warnings &&
          cargo clippy --locked --manifest-path crates/lgwks-bot/Cargo.toml
          --all-targets --no-default-features -- -D warnings &&
          cargo clippy --locked --manifest-path crates/lgwks-ast/Cargo.toml
          --all-targets --no-default-features -- -D warnings

  docs:
    name: Docs
    needs: [build]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Pin Rust 1.98.0
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.98.0"
      - name: Doc build with broken-link denial
        # Per-crate (gpui excluded: it needs the Xcode Metal toolchain and is
        # covered by the gpui-macos job, not ubuntu lanes).
        run: >
          RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --locked
          -p lgwks_std --all-features &&
          RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --locked
          -p lgwks_bot --all-features &&
          RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --locked
          -p lgwks_ast --all-features &&
          RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --locked
          -p lgwks_deps --features tokio-full
      - name: Doc build without default features
        # lgwks_std is included because a module in `core` that links to a
        # feature-gated module resolves under `--all-features` and breaks here.
        # retry linked to `random` and only this lane could see it.
        run: >
          RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --locked
          --manifest-path crates/lgwks-std/Cargo.toml --no-default-features &&
          RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --locked
          --manifest-path crates/lgwks-bot/Cargo.toml --no-default-features &&
          RUSTDOCFLAGS='-D warnings' cargo doc --no-deps --locked
          --manifest-path crates/lgwks-ast/Cargo.toml --no-default-features
      - name: docs.rs metadata names a buildable feature set
        # docs.rs builds with whatever `[package.metadata.docs.rs]` declares,
        # and a failure there is invisible from the repository: the crate page
        # just shows a build error. `all-features = true` on a crate whose
        # features include platform-bound ones fails on every publish, which is
        # what happened to lgwks_deps: `gpui` pulls `objc2`, which refuses to
        # compile off Apple targets. Build with the declared set so an
        # unbuildable declaration fails here instead of on docs.rs.
        run: |
          set -eu
          python - <<'PY' > /tmp/docsrs_features.sh
          import tomllib
          from pathlib import Path

          for manifest in sorted(Path("crates").glob("*/Cargo.toml")):
              parsed = tomllib.loads(manifest.read_text(encoding="utf-8"))
              # `[package.metadata.docs.rs]` parses as metadata["docs"]["rs"].
              meta = (
                  parsed.get("package", {})
                  .get("metadata", {})
                  .get("docs", {})
                  .get("rs")
              )
              if meta is None:
                  continue
              name = parsed["package"]["name"]
              if meta.get("all-features"):
                  print(f"# {name}: all-features")
                  print(f'cargo doc --no-deps --locked -p {name} --all-features')
              else:
                  feats = meta.get("features", [])
                  joined = ",".join(feats)
                  print(f"# {name}: {joined or '(defaults only)'}")
                  if feats:
                      print(f'cargo doc --no-deps --locked -p {name} --features {joined}')
                  else:
                      print(f'cargo doc --no-deps --locked -p {name}')
          PY
          cat /tmp/docsrs_features.sh
          RUSTDOCFLAGS='-D warnings --cfg docsrs' sh -e /tmp/docsrs_features.sh

      - name: Root README quickstart matches its compiled example
        run: |
          python - <<'PY'
          import re
          from pathlib import Path

          # README.md at the repository root is not a crate README, so nothing
          # type-checks its code blocks; only crates/lgwks-bot/README.md is
          # compiled, via include_str! in lib.rs. The quickstart is mirrored in
          # an example that `cargo test --all-targets` does compile, and this
          # keeps the two identical, so an API change breaks the build instead
          # of silently invalidating the first code a new user copies.
          readme = Path("README.md").read_text(encoding="utf-8")
          section = readme.split("## Quickstart", 1)[1].split("\n## ", 1)[0]
          blocks = re.findall(r"```rust\n(.*?)```", section, re.S)
          if len(blocks) != 1:
              raise SystemExit(
                  f"expected one rust block under Quickstart, found {len(blocks)}"
              )
          readme_block = blocks[0].strip()

          example = Path(
              "crates/lgwks-bot/examples/quickstart.rs"
          ).read_text(encoding="utf-8")
          body = "\n".join(
              line for line in example.split("\n") if not line.startswith("//!")
          ).strip()

          if readme_block != body:
              raise SystemExit(
                  "README quickstart drifted from "
                  "crates/lgwks-bot/examples/quickstart.rs:\n"
                  f"--- README.md ---\n{readme_block}\n"
                  f"--- quickstart.rs ---\n{body}"
              )
          print("README quickstart matches the compiled example")
          PY

  feature-matrix:
    name: lgwks-std feature matrix
    needs: [build]
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Pin Rust 1.98.0
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.98.0"
      - name: Check lgwks-std feature matrix
        run: |
          set -eu
           cargo check --locked --manifest-path crates/lgwks-std/Cargo.toml --all-targets --no-default-features
           cargo check --locked --manifest-path crates/lgwks-std/Cargo.toml --all-targets
           cargo check --locked --manifest-path crates/lgwks-std/Cargo.toml --all-targets --features hash --no-default-features
           cargo check --locked --manifest-path crates/lgwks-std/Cargo.toml --all-targets --features pattern --no-default-features
           cargo check --locked --manifest-path crates/lgwks-std/Cargo.toml --all-targets --features json --no-default-features
           cargo check --locked --manifest-path crates/lgwks-std/Cargo.toml --all-targets --features wire --no-default-features
           cargo check --locked --manifest-path crates/lgwks-std/Cargo.toml --all-targets --features http --no-default-features
           cargo check --locked --manifest-path crates/lgwks-std/Cargo.toml --all-targets --features online --no-default-features
           cargo check --locked --manifest-path crates/lgwks-std/Cargo.toml --all-targets --features fs-raw --no-default-features
            cargo check --locked --manifest-path crates/lgwks-std/Cargo.toml --all-targets --all-features
      - name: Check lgwks-deps
        run: cargo check --locked --manifest-path crates/lgwks-deps/Cargo.toml --all-targets
      - name: Check lgwks-deps storefront engine feature
        run: cargo check --locked --manifest-path crates/lgwks-deps/Cargo.toml --all-targets --features tokio
      - name: Check lgwks-deps GPUI storefront feature
        run: cargo check --locked --manifest-path crates/lgwks-deps/Cargo.toml --all-targets --features gpui
      - name: Check lgwks-deps ML storefront features
        run: cargo check --locked --manifest-path crates/lgwks-deps/Cargo.toml --all-targets --features ml-candle,ml-tokenizers
      - name: Check lgwks-bot async feature matrix
        run: |
          set -eu
           cargo check --locked --manifest-path crates/lgwks-bot/Cargo.toml --all-targets --no-default-features
           cargo check --locked --manifest-path crates/lgwks-bot/Cargo.toml --all-targets
           cargo check --locked --manifest-path crates/lgwks-bot/Cargo.toml --all-targets --features net,process,fs,signal
           cargo check --locked --manifest-path crates/lgwks-bot/Cargo.toml --all-targets --all-features
      - name: Check lgwks-bot WASI runtime boundary
        run: |
          set -eu
          rustup target add wasm32-wasip1
           cargo check --locked --manifest-path crates/lgwks-bot/Cargo.toml --target wasm32-wasip1 --features rt
      - name: Check lgwks-ast feature matrix
        run: |
          set -eu
           cargo check --locked --manifest-path crates/lgwks-ast/Cargo.toml --all-targets --no-default-features
           cargo check --locked --manifest-path crates/lgwks-ast/Cargo.toml --all-targets
           cargo check --locked --manifest-path crates/lgwks-ast/Cargo.toml --all-targets --all-features

  appcui-native:
    name: AppCUI native storefront
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    strategy:
      matrix:
        os: [ubuntu-latest, macos-14, windows-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.98.0"
          components: clippy
      - run: cargo test --locked -p lgwks_deps --no-default-features --features appcui --lib
      - run: cargo test --locked -p lgwks_deps --no-default-features --features appcui --doc
      - run: cargo clippy --locked -p lgwks_deps --no-default-features --features appcui --all-targets -- -D warnings

  gpui-macos:
    name: GPUI macOS renderer
    needs: [build]
    runs-on: macos-14
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Pin Rust 1.98.0
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.98.0"
      - name: Check normal GPUI renderer feature
        run: cargo check --locked -p lgwks_deps --all-targets --features gpui

  candle-macos:
    name: Candle ML storefront (Metal)
    needs: [build]
    runs-on: macos-14
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
      - name: Pin Rust 1.98.0
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.98.0"
      - name: Check Candle Metal storefront features
        run: cargo check --locked -p lgwks_deps --all-targets --features ml-candle-metal,ml-tokenizers

  contract-drift:
    name: lgwks-std contract drift checks
    needs: [build]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Validate the dependency contract alignment
        run: |
          python - <<'PY'
          import re
          import tomllib
          from pathlib import Path

          manifest = Path("crates/lgwks-std/Cargo.toml")
          readme = Path("crates/lgwks-std/README.md")

          parsed = tomllib.loads(manifest.read_text(encoding="utf-8"))
          manifest_deps = set(parsed["dependencies"].keys())

          text = readme.read_text(encoding="utf-8")
          marker = "## Dependency philosophy"
          start = text.find(marker)
          if start == -1:
              raise SystemExit("README dependency philosophy section missing")

          section = text[start:start + 1800]
          readme_deps = set(re.findall(r"- \*\*([a-zA-Z0-9_-]+)\*\*", section))

          missing_from_readme = sorted(manifest_deps - readme_deps)
          missing_from_manifest = sorted(readme_deps - manifest_deps)
          if missing_from_readme or missing_from_manifest:
              raise SystemExit(
                  f"Dependency contract drift detected. "
                  f"missing_from_readme={missing_from_readme}, "
                  f"missing_from_manifest={missing_from_manifest}"
              )

          package = parsed["package"]
          if package["repository"] != "https://github.com/srinji-kaggss/logicalworks-crates":
              raise SystemExit(
                  f"Unexpected package repository URL: {package['repository']}"
              )

          # README version pins must track the manifests (stale copy-paste
          # downgrades consumers on 0.x, where minors may break).
          import re as _re

          pins = {
              "lgwks_std": tomllib.loads(
                  Path("crates/lgwks-std/Cargo.toml").read_text(encoding="utf-8")
              )["package"]["version"],
              "lgwks_bot": tomllib.loads(
                  Path("crates/lgwks-bot/Cargo.toml").read_text(encoding="utf-8")
              )["package"]["version"],
              "lgwks_ast": tomllib.loads(
                  Path("crates/lgwks-ast/Cargo.toml").read_text(encoding="utf-8")
              )["package"]["version"],
              "lgwks_deps": tomllib.loads(
                  Path("crates/lgwks-deps/Cargo.toml").read_text(encoding="utf-8")
              )["package"]["version"],
          }
          md_files = [Path("README.md")] + sorted(Path("crates").glob("*/README.md"))
          bad = []
          for md in md_files:
              text = md.read_text(encoding="utf-8")
              for crate, version in pins.items():
                  major, minor, *_ = version.split(".")
                  for m in _re.finditer(
                      rf"{crate}\s*=\s*(?:\{{\s*version\s*=\s*)?\"(\d+)\.(\d+)(?:\.(\d+))?\"",
                      text,
                  ):
                      if (m.group(1), m.group(2)) != (major, minor):
                          bad.append(f"{md}: {m.group(0)} (manifest is {version})")
                  # Markdown table rows, as in the root README crate table:
                  # | `lgwks_std` | 0.6.4 | [docs.rs](...) | ...
                  for m in _re.finditer(
                      rf"\|\s*`{crate}`\s*\|\s*(\d+)\.(\d+)(?:\.(\d+))?\s*\|",
                      text,
                  ):
                      if (m.group(1), m.group(2)) != (major, minor):
                          bad.append(f"{md}: {m.group(0)} (manifest is {version})")
          if bad:
              raise SystemExit("Stale README version pins:\n" + "\n".join(bad))
          print("README version pins track the manifests")
          PY

  package-smoke:
    name: lgwks-std package smoke-test
    needs: [build]
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Pin Rust 1.98.0
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.98.0"
      - name: Run package smoke test
        run: ./scripts/lgwks-std-package-smoke.sh
